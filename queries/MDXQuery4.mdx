--Does the classroom where the exam has been taken influence the average results?
SELECT
    ORDER([Dim Classroom].[Room Number].MEMBERS, [Measures].[Average_score_exam], BDESC) ON ROWS,
    [Measures].[Average_score_exam] ON COLUMNS
FROM
    [School DW];
